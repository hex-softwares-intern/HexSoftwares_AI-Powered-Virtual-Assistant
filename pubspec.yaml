name: ai_assistant
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  # Dependencies specify other packages that your package needs in order to work.
  # To automatically upgrade your package dependencies to the latest versions
  # consider running `flutter pub upgrade --major-versions`. Alternatively,
  # dependencies can be manually updated by changing the version numbers below to
  # the latest version available on pub.dev. To see which dependencies have newer
  # versions available, run `flutter pub outdated`.
  # ── Networking ────────────────────────────────────
  # Dio: powerful HTTP client with interceptors, timeouts, cancellation
  # pretty_dio_logger: prints beautiful request/response logs in debug
  dio: ^5.9.2
  pretty_dio_logger: ^1.4.0

  # ── State Management ──────────────────────────────
  # Riverpod 3.x: latest, works on Flutter beta
  # riverpod_annotation: enables @riverpod code generation syntax
  flutter_riverpod: ^3.3.1
  riverpod_annotation: ^4.0.2

  # ── Voice I/O ─────────────────────────────────────
  # speech_to_text: microphone → text, works on all platforms
  # flutter_tts: text → speech, supports Android/iOS/macOS/Windows
  flutter_sound: ^9.2.13
  flutter_onnxruntime: ^1.6.3 # ── Local Database ────────────────────────────────
  # drift: type-safe SQLite ORM, all platforms, reactive streams
  # drift_flutter: Flutter-specific db file opener for drift
  # path_provider: finds correct db directory per platform
  drift: ^2.20.3
  drift_flutter: ^0.2.2
  record: ^6.2.0
  path_provider: ^2.1.5
  path: ^1.9.0

  # ── Simple Key-Value Storage ──────────────────────
  # For non-sensitive settings: preferred music app, theme, language
  shared_preferences: ^2.5.4

  # ── Secure Encrypted Storage ──────────────────────
  # For sensitive data: JWT token, Groq API key, user credentials
  # Uses Keychain (iOS), Keystore (Android), DPAPI (Windows)
  flutter_secure_storage: ^10.0.0

  # ── Local Notifications ───────────────────────────
  # Custom alarm + reminder system — no OS app dependency
  # timezone: required for scheduling notifications at exact local time
  flutter_local_notifications: ^21.0.0
  timezone: ^0.11.0

  # ── Platform / App Interaction ────────────────────
  # url_launcher: open URLs and deep links cross-platform
  # android_intent_plus: fire Android Intents (music, maps, camera)
  # permission_handler: runtime permissions (mic, calendar, storage)
  url_launcher: ^6.3.2
  android_intent_plus: ^6.0.0
  permission_handler: ^10.2.0
  webview_flutter: ^4.13.1

  # ── Google Auth + Calendar ────────────────────────
  # google_sign_in: OAuth2 login — Flutter team official package
  # googleapis: Google Calendar, Gmail, Drive REST API client
  # http: required by googleapis for making API calls
  google_sign_in: ^7.2.0
  googleapis: ^13.2.0
  http: ^0.13.5

  # ── Model / Serialization ─────────────────────────
  # freezed_annotation: marks classes for immutable model generation
  # json_annotation: marks classes for toJson/fromJson generation
  freezed_annotation: ^3.1.0
  json_annotation: ^4.11.0
  location: ^8.0.1
  vosk_flutter: ^0.3.48
  # ── Utilities ─────────────────────────────────────
  # uuid: generate unique IDs for alarms, reminders, messages
  # intl: date/time formatting, localization
  uuid: ^4.5.3
  intl: ^0.20.2

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  # ── Code Generation ───────────────────────────────
  # build_runner: runs all code generators below
  # riverpod_generator: generates Provider boilerplate from @riverpod
  # freezed: generates copyWith, ==, toString for immutable models
  # json_serializable: generates toJson/fromJson for API models
  # drift_dev: generates type-safe DB queries from table definitions
  build_runner: ^2.4.13
  riverpod_generator: ^4.0.3
  freezed: ^3.2.5
  json_serializable: ^6.13.0
  drift_dev: ^2.20.3

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  assets:
    - assets/vosk-model/
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
dependency_overrides:
  permission_handler: ^12.0.1
  http: ^1.6.0
